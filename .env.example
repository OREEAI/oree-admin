# Base URL of the Oree backend API. Mirrors the customer app convention:
# the value is trimmed of a trailing slash and falls back to the production
# API host when unset (see service/api.ts).
NEXT_PUBLIC_API_URL=https://api.oreeai.com
